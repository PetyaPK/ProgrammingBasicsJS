function characterSequence(text) {
    for (let i = 0; i < text.length; i++) {
        let letter = text[i];
        console.log(letter);
        
    }
}



characterSequence(`softuni`)
                // 0123456